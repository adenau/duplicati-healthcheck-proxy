apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "healthcheck-proxy.fullname" . }}-test-connection"
  labels:
{{ include "healthcheck-proxy.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "healthcheck-proxy.fullname" . }}:{{ .Values.service.port }}/health']
  restartPolicy: Never
